<!DOCTYPE html>
<html lang="<%= lang %>">
  <head>
    <meta charset="utf-8" />
    <meta name="viewport" content="width=device-width, initial-scale=1" />
    <title><%= title %></title>
    <link rel="stylesheet" href="/stylesheets/style.css" />
  </head>
  <body>
    <main>
      <%- include('partials/lang-switch') %>
      <h1><%= t.homeTitle %></h1>
      <p><%= t.homeLead %></p>
      <p class="side"><%= side.homeLead %></p>
      <p><%= t.homeBody %></p>
      <p class="side"><%= side.homeBody %></p>
      <p><a href="<%= withLang('/form') %>"><%= t.homeCta %></a></p>
    </main>
  </body>
</html>
